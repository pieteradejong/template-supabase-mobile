/**
 * @acme/utils - Shared utilities
 *
 * This package exports shared utilities, validation schemas,
 * and helper functions used across the monorepo.
 */

export { envSchema, validateEnv, getEnv, type EnvConfig } from "./env";
